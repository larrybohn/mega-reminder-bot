import React, { Component } from 'react';

export class Reminders extends Component {
    render() {
        return (
            <div className="mt-4">
                Note that the Reminder Bot does not have access to the contents of your reminders.
            </div>
        );
    }
}

export default Reminders;